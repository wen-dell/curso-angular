import { CampoCresceDirective } from './campo-cresce.directive';

describe('CampoCresceDirective', () => {
  it('should create an instance', () => {
    const directive = new CampoCresceDirective();
    expect(directive).toBeTruthy();
  });
});
