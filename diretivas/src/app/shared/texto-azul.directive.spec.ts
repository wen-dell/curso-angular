import { TextoAzulDirective } from './texto-azul.directive';

describe('TextoAzulDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoAzulDirective();
    expect(directive).toBeTruthy();
  });
});
